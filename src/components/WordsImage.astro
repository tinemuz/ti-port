---
import { Image } from "astro:assets";

interface Props {
  src: ImageMetadata;  // Change type to ImageMetadata for asset handling
  alt: string;
  class?: string;
}

const { src, alt } = Astro.props;
---

<Image
  src={src}
  alt={alt}
  class="rounded-lg border-2 border-skin-border/50"
/>