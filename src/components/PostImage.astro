---
import { Image } from "astro:assets";

interface Props {
  src: ImageMetadata;
  alt: string;
  class?: string;
}

const { src, alt } = Astro.props;
---

<Image
  src={src}
  alt={alt}
  class="rounded-lg border border-skin-border/50"
/>